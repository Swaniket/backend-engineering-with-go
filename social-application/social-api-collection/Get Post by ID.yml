info:
  name: Get Post by ID
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/v1/posts/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
