info:
  name: User Feed
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/v1/users/feed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
