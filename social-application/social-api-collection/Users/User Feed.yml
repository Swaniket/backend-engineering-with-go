info:
  name: User Feed
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/v1/users/feed?limit=10&sort=asc&tags=innovation
  params:
    - name: limit
      value: "10"
      type: query
    - name: sort
      value: asc
      type: query
    - name: search
      value: morning
      type: query
      disabled: true
    - name: tags
      value: innovation
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
