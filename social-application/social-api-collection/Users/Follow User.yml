info:
  name: Follow User
  type: http
  seq: 2

http:
  method: PUT
  url: http://localhost:8080/v1/users/83/follow
  body:
    type: json
    data: |-
      {
        "user_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
